<!DOCTYPE html>
<html lang="fr">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{% block title %}Application{% endblock %}</title>
	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
	<!-- FontAwesome -->
	<script src="https://kit.fontawesome.com/1038d5e71f.js" crossorigin="anonymous"></script>
	<link rel="stylesheet" href="assets/css/template.css">
	{% block css %}{% endblock %}
</head>
<body>
	{% if not hideHeader|default(false) %}
	<nav class="navbar">
		<div class="container-fluid d-flex justify-content-between align-items-center h-100">
			<!-- Bouton Accueil -->
			<a href="/accueil" class="btn btn-light d-flex align-items-center gap-2 px-3 py-2 rounded-pill shadow-sm">
				<i class="fas fa-home"></i>
				<span class="fw-semibold d-none d-md-inline">Accueil</span>
			</a>
			<!-- Texte et icône admin au centre -->
			<div class="d-flex flex-column align-items-center">
				<div class="d-flex align-items-center gap-2">
					<i class="fas fa-user text-white fs-3"></i>
					<span class="text-white fw-semibold fs-5">
                        {% if userName %}
                            {{ userPrenom }} {{ userName }}
                        {% else %}
                            Non connecté
                        {% endif %}
                    </span>
				</div>
			</div>
			<!-- Bouton Déconnexion -->
			<a href="/logout" class="btn btn-outline-light d-flex align-items-center gap-2 px-3 py-2 rounded-pill shadow-sm">
				<i class="fas fa-sign-out-alt"></i>
				<span class="fw-semibold d-none d-md-inline">Déconnexion</span>
			</a>
		</div>
	</nav>
	{% endif %}

	<main class="container" style="margin-top:130px;">
		{% block body %}{% endblock %}
	</main>

	<footer>
		<div class="container d-flex justify-content-between align-items-center">
			<div class="footer-logo">
				<img src="assets/images/IUT-Havre.png" alt="IUT Le Havre">
			</div>
			<div>
				BUT Informatique - Le Havre © 2025
			</div>
			<div class="footer-space"></div>
		</div>
	</footer>

	<!-- Bootstrap Bundle JS -->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
	{% block javascript %}{% endblock %}
</body>
</html>